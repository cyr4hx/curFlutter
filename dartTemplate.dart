import 'package:flutter/material.dart';

void main() => runApp( Ceo() );

class Ceo extends StatelessWidget
{
  @override
  Widget build( BuildContext context )
  {
    return MateriallApp
           (
             home: Scaffold
                   (
                     appBar: AppBar
                             (
                               
                             ),
                     drawer: Drawer
                             (
                               
                             ),
                     body: Column
                           (
                             
                           ),
                   ),
           );
  }
}
